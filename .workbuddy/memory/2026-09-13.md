# 2026-09-13

## 核查 Git 安装状态（用户要求「安装 git」）

结论：**无需安装，已就绪**。

- `/usr/bin/git`，版本 `2.50.1 (Apple Git-155)`，由 Xcode Command Line Tools 提供
- CLT 路径 `/Library/Developer/CommandLineTools`，安装于 2026-09-07，占 1.9 GB
- 功能实测通过：`git init` + `git add` + `git commit` 均正常，仓库创建后默认分支为 `main`

已具备的系统级配置（来源 `/Library/Developer/CommandLineTools/usr/share/git-core/gitconfig`）：

```
[credential] helper = osxkeychain
[init]       defaultBranch = main
```

**唯一缺口：没有任何用户级配置**，`~/.gitconfig` 不存在，`user.name` / `user.email` 均未设置 —— 这会导致 commit 失败或作者信息错误。已向用户索要姓名与邮箱待配置。

环境事实：本机无 Homebrew，`/opt/homebrew/bin/git` 与 `/usr/local/bin/git` 均不存在，git 只有 CLT 这一个来源。

## 配置 Git 身份（已完成）

- GitHub 用户名 `ZiqianStars`，数字 ID `64132382`（账号注册于 2020-04-22），资料显示名 `Xu Weldon`
- 用户选定场景：**要推到 GitHub**，因此邮箱用 noreply 形式保护真实邮箱
- 最终配置（`~/.gitconfig`）：
  - `user.name` = `ZiqianStars`
  - `user.email` = `64132382+ZiqianStars@users.noreply.github.com`
- 验证通过：真实提交作者显示为 `ZiqianStars <64132382+ZiqianStars@users.noreply.github.com>`

### 踩坑记录（重要）

**坑 1：`git config --global` 写入失败**

报错 `error: could not lock config file ~/.gitconfig: File exists`。根因是 git 的写入流程是「先写 `.gitconfig.lock` 临时文件 → 原子重命名」，沙箱拦截了重命名这一步，导致残留的锁文件把后续所有写入堵死（残留内容正是半截的 `[user] name = ...`）。

- 排查要点：`pgrep -fl git` 会匹配到微信子进程（路径含 "git"），**不能据此判断有 git 进程占用锁**
- 解决：`mv ~/.gitconfig.lock /tmp/` 移走残留锁，然后**用 Write 工具直接写 `~/.gitconfig`**，绕开加锁流程

**坑 2：未配置身份时 git 不报错，静默用回退值**

实测未配置时提交照常成功，作者被静默写成 `wenjuxu <wenjuxu@MacBook-Air.local>`（取自系统账户名 + 主机名）。这类错误不报错、只写进历史，最难发现。

### 方法论：如何拿到正确的 noreply 邮箱

不需要访问 `github.com`（本机不通），走 `api.github.com` 即可：

```bash
curl -sL "https://api.github.com/users/<用户名>" | python3 -c "
import sys, json; d=json.load(sys.stdin)
print(f\"{d['id']}+{d['login']}@users.noreply.github.com\")"
```

注意 `users.noreply.github.com` 域名解析不通是**正常的** —— 它不是真实邮箱，只是绑定账号 ID 的标识符。

